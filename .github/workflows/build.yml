name: build

on:
  push:
    branches: [ "**" ]
  pull_request:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Set up Android SDK
        run: |
          echo "ANDROID_HOME=/usr/local/lib/android/sdk" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=/usr/local/lib/android/sdk" >> "$GITHUB_ENV"

      - name: Install SDK packages
        run: |
          yes | /usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager \
            "platforms;android-36" "build-tools;36.0.0" "platform-tools"

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Unit tests + lint + assemble debug & release APK
        run: ./gradlew testDebugUnitTest lintDebug assembleDebug assembleRelease --no-daemon --stacktrace

      - name: Upload debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/*.apk

      - name: Upload release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: app/build/outputs/apk/release/*.apk

      - name: Prepare release APK (main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          VN=$(grep 'versionName = ' app/build.gradle.kts | head -1 | sed -E 's/.*"([^"]+)".*/\1/')
          echo "APP_TAG=v$VN" >> "$GITHUB_ENV"
          mkdir -p dist
          cp app/build/outputs/apk/release/app-release.apk "dist/GalaxyAlarm-v$VN.apk"

      - name: Publish to GitHub Releases (main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.APP_TAG }}
          name: Galaxy Clock ${{ env.APP_TAG }}
          files: dist/*.apk
          body: |
            Galaxy Clock ${{ env.APP_TAG }}

            APK: GalaxyAlarm-${{ env.APP_TAG }}.apk

            スマホのブラウザでこのページを開き、APKをダウンロードしてインストールしてください。

      - name: Verify published APK URL (main only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          URL="https://github.com/${{ github.repository }}/releases/download/${APP_TAG}/GalaxyAlarm-${APP_TAG}.apk"
          for i in {1..12}; do
            status="$(curl -L -s -o /dev/null -w '%{http_code}' "$URL")"
            if [ "$status" = "200" ]; then
              echo "Verified $URL"
              exit 0
            fi
            echo "Waiting for APK asset ($status): $URL"
            sleep 10
          done
          echo "APK asset was not reachable: $URL" >&2
          exit 1
